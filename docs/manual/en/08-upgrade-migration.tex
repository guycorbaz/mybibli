\chapter{Upgrade and migration}
\label{ch:upgrade}

Routine upgrades are designed to be safe. The exception is the
1.0.x to 1.1.0 jump, which is a one-time breaking change documented
below.

\section{Routine upgrades (1.y to 1.z)}

\begin{enumerate}
  \item Read the release notes for every version between your
        current and the target version.
  \item Take a fresh \textbf{database backup} (chapter
        \ref{ch:backup}).
  \item Update the image tag in \texttt{docker-compose.yml} (or
        repull \texttt{:latest} if you track that tag).
  \item Run \texttt{docker compose up -d}. The new container boots,
        runs any new migrations against the existing database, and
        starts serving requests.
  \item Watch the logs for migration errors:
        \texttt{docker compose logs mybibli | grep -i migrate}.
\end{enumerate}

Migrations are append-only — schema changes are forward
compatible. A 1.4 install upgraded to 1.5 keeps every row from 1.4
and applies new tables / columns in place.

\begin{tip}
For homelab installs, the \texttt{:latest} Docker tag is a
reasonable default. For production-style setups (a community
library, an association), pin to an explicit version tag so an
unattended pull does not bring in a release you have not read about.
\end{tip}

\section{Skipping intermediate versions}

Going from a much older install (say, 1.3.x) directly to the
current release is supported and safe for the database. The
migration runner applies every pending migration in timestamp
order at boot, regardless of how many you skip. Schema
migrations are purely additive — no \texttt{DROP COLUMN}, no
\texttt{DROP TABLE} in any release — and the four data
backfills that exist (e.g. the 1.1.0 CSRF token seed, the
1.1.8 \texttt{manually\_edited\_fields} cleanup) are
idempotent.

What still applies on a multi-version jump:

\begin{itemize}
  \item \textbf{Back up before upgrading.} There is no automatic
        rollback. A migration that fails mid-execution leaves the
        database in an inconsistent state; the recovery path is the
        pre-upgrade backup (chapter~\ref{ch:backup}).
  \item \textbf{Pre-1.1.4 cover JPGs are gone.} Until 1.1.4 the
        \texttt{mybibli\_covers} volume was not declared, so cover
        images written before that version were lost on every
        container upgrade. Skipping straight to 1.7 does not
        recover them — re-fetch via the admin \textbf{Bulk
        re-fetch missing covers} action (added in 1.2) or the
        per-title \textbf{Re-fetch metadata} button.
  \item \textbf{The first boot may take a few minutes.} A jump
        that runs 20+ migrations against a large catalog is
        silent at the UX level — wait for \texttt{/health} to
        respond before worrying.
\end{itemize}

The 1.0.x to 1.1.0 jump is the one exception that needs the
dedicated procedure below.

\section{The 1.0.x to 1.1.0 jump}\label{sec:upgrade-1.1.0}

\begin{warning}
\textbf{This is a one-time breaking change.}\index{1.1.0 upgrade}
1.0.x installs are subject to the issue documented in
\href{https://github.com/guycorbaz/mybibli/issues/173}{\#173}:
the seed migrations create the credentials \texttt{admin/admin}
and \texttt{librarian/librarian} on every fresh install, including
production, and silently bypass the first-launch setup wizard.
\end{warning}

The recommended path depends on whether your 1.0.x install holds
real data:

\subsection*{If your 1.0.x install is empty (no titles cataloged)}

The simplest path: wipe the database and reinstall on 1.1.0+.

\begin{enumerate}
  \item Stop the stack: \texttt{docker compose down}.
  \item Remove the database volume:
        \texttt{docker volume rm mybibli\_mybibli\_db\_data}
        (or the equivalent volume name on your host —
        \texttt{docker volume ls} lists them).
  \item Pull the 1.1.0 image:
        \texttt{docker compose pull}.
  \item Update the image tag in \texttt{docker-compose.yml} if you
        pinned a previous version.
  \item Start: \texttt{docker compose up -d}.
  \item Open \texttt{/setup} and walk through the wizard.
\end{enumerate}

\subsection*{If your 1.0.x install holds real data}

\begin{enumerate}
  \item Take a database backup (chapter \ref{ch:backup}).
  \item Take a covers + \texttt{.env} backup.
  \item Log in as the seeded admin and rotate the password
        \emph{before} you upgrade. The 1.1.0 upgrade flow itself is
        safe, but rotation closes the only window where the default
        credentials could leak.
  \item Update the image tag in \texttt{docker-compose.yml} and run
        \texttt{docker compose up -d}.
  \item The 1.1.0 boot detects the existing admin and does
        \emph{not} re-enable the wizard — the rotated admin remains
        the active admin.
\end{enumerate}

\subsection*{The new \texttt{MYBIBLI\_SEED\_DEV\_USERS} variable}

Starting with 1.1.0, the seed migration is gated by
\texttt{MYBIBLI\_SEED\_DEV\_USERS}\index{MYBIBLI\_SEED\_DEV\_USERS}.
The default is \texttt{0} (no seed). Only set it to \texttt{1} for
local-development workflows or automated tests — never in
production. The first-launch wizard becomes the canonical
onboarding path.

\section{Reading the release notes}

Every mybibli release publishes a GitHub Release page with:

\begin{itemize}
  \item a one-paragraph summary of the change set;
  \item the migration impact (none / additive / breaking);
  \item a link to the diff against the previous tag;
  \item attached PDFs of this manual (English + French) once the
        release-time PDF build is wired into CI.
\end{itemize}

Read the notes before upgrading. Breaking changes are tagged with a
prominent \emph{Breaking} label.

\section{What's new in 1.7.11}\label{sec:whats-new-1.7.11}

Version 1.7.11 bundles one user-visible WCAG AA contrast fix and three
code-review-finding clean-ups, plus a companion contrast fix surfaced
during the v1.7.11 local Rule~13 gate. \emph{No schema migration, no
settings change}; routine \texttt{docker compose pull \&\& docker compose
up -d}.

\textbf{User-visible (accessibility):}

\begin{itemize}
  \item \textbf{\#345 \string— trash count admin badge contrast bumped
        to WCAG~AA.} The badge in \texttt{templates/components/admin\_tabs.html}
        paired \texttt{bg-red-500} (\#fb2c36) with \texttt{text-white},
        yielding only 3.8:1 — below the WCAG~2.2~AA 4.5:1 threshold for
        normal text. Switched to \texttt{bg-red-700} (\#b91c1c, 6.61:1)
        — comfortably above AA and above the AAA 7.0 threshold. Badge
        stays visually a ``warning red''. New
        \texttt{templates\_audit::templates\_forbid\_bg\_red\_500\_with\_text\_white}
        regression guard panics on any future template pairing the two
        — the combination is intrinsically inaccessible, so a static
        audit is the right enforcement level.
  \item \textbf{\#352 (companion) \string— locations tree L-code + node-type
        spans contrast.} Same shape as \#345, different element. The
        L-code span on \texttt{/locations} (e.g. \texttt{L1004}) and
        the V-code span on \texttt{/location/:id} used naked
        \texttt{text-stone-400} (\#a8a29e) on \texttt{bg-stone-50}
        (\#fafaf9) light mode \string= 2.41:1. Switched to
        \texttt{text-stone-600 dark:text-stone-400} for 7.5:1 on light
        and 8:1 on dark.
\end{itemize}

\textbf{Code-review-finding clean-ups (developer-visible):}

\begin{itemize}
  \item \textbf{\#30 \string— CI/tooling.} \texttt{.github/workflows/release.yml}
        now extracts the Cargo.toml version via Python's stdlib
        \texttt{tomllib} (3.11+, available on ubuntu-latest) instead of
        \texttt{grep}, making it robust against UTF-8 BOM, CRLF,
        single-quoted values, and a future \texttt{[workspace.package]}
        section. \texttt{tests/e2e/tsconfig.json} adds
        \texttt{noUncheckedIndexedAccess} +
        \texttt{exactOptionalPropertyTypes}; the 12 surfaced narrow
        non-null assertions on regex-match indexing are backfilled.
        The CI typecheck (e2e job) now catches any future violation.
        Sub-items 2 (tag-mismatch smoke test) and 3 (seed-migration
        drift audit) deferred to their own focused issues.
  \item \textbf{\#340 \string— TEST\_MODE seed-overdue-loan endpoint.}
        New \texttt{POST /debug/seed-overdue-loan} (TEST\_MODE-gated,
        Admin role check, mirror of \texttt{debug\_set\_session\_timeout})
        that backdates \texttt{loans.loaned\_at} via direct SQL — UI
        seeding cannot travel time. Closes the overdue branch of CR
        \#119: \texttt{home.spec.ts} can finally seed a real overdue
        loan and assert the indicator end-to-end, removing the silent
        \texttt{test.skip} that previously green'd a missing-indicator
        regression.
  \item \textbf{\#35 slice \string— \texttt{base\_context()} DRY helper.}
        New \texttt{BaseContextFields} struct in \texttt{src/utils.rs}
        carries the 20 fields every full-page template struct repeats
        (lang, role, csrf\_token, 10 nav strings, …). Built by
        \texttt{base\_context(\&session, locale, current\_page, \&uri,
        session\_timeout\_secs)} and applied to the
        \texttt{loans\_page} / \texttt{borrowers\_page} /
        \texttt{series\_index} handlers as proof-of-pattern. Reduces
        i18n key duplication: a typo on \texttt{nav.catalog} now
        surfaces in one place, not 17. Remaining $\sim$14 full-page
        handlers + the deferred sub-items
        (\texttt{generate\_session\_token} dedup,
        \texttt{Session::anonymous\_with\_token} fallback removal) are
        tracked in a follow-up issue for incremental migration.
\end{itemize}

\section{What's new in 1.7.10}\label{sec:whats-new-1.7.10}

Version 1.7.10 is a single-fix patch closing a silent-data-loss
vulnerability. \emph{No schema migration, no settings change}; routine
\texttt{docker compose pull \&\& docker compose up -d}.

\textbf{User-visible:}

\begin{itemize}
  \item \textbf{\#347 \string— manually uploaded covers are no longer
        overwritten by the metadata-fetch chain.} The async background
        fetch after the initial scan, and the bulk-refetch admin action
        in a race with a fresh upload, could silently overwrite a
        manually uploaded cover (the v1.7.6 \#335 safety net) because
        \texttt{update\_cover\_image\_url} did an unconditional UPDATE
        that ignored \texttt{manually\_edited\_fields}. The failure
        path was equally vulnerable, blanking the manual cover to NULL
        when the provider download failed. The fix is a 22-line guard
        pattern identical to \texttt{do\_update}'s with 4 integration
        tests covering success / download-failure / regression /
        specificity.
\end{itemize}

\textbf{Companion finding (no code change):}

The broader cover-miss gap on the production NAS (73/147 titles,
\textasciitilde50\,\%) is structural and not closable from the
provider chain. Google Books returns
\texttt{totalItems\,=\,0} for the dominant FR/CH/DE tech publishers
(Dunod, Eyrolles, Slatkine, Mardaga), and Open Library Covers does
not index their ISBNs either. The v1.7.9 piste-1 fix
(highest-resolution \texttt{imageLinks} variant) was prod-validated
as ineffective at moving that count. Manual upload (\#335) remains
the only realistic safety net for those titles. A v1.8 UX track to
make the upload affordance more visible is being scoped.

\section{What's new in 1.7.9}\label{sec:whats-new-1.7.9}

Version 1.7.9 is a cover-quality + admin-UX + 4-hardening bundle. One
user-visible cover-quality fix, one admin-visible config surface, and
four code-review-finding hardenings. \emph{Two new K/V settings rows
(seeded at the prior hardcoded defaults), no breaking changes}; routine
\texttt{docker compose pull \&\& docker compose up -d}.

\textbf{User-visible:}

\begin{itemize}
  \item \textbf{\#333 piste 1 \string— Google Books cover URL prefers
        the highest-resolution \texttt{imageLinks} variant.} The
        provider used to read only \texttt{thumbnail} from the Google
        Books response, even when the payload also carried
        \texttt{small} / \texttt{medium} / \texttt{large} /
        \texttt{extraLarge}. The downstream 400\,px Lanczos resize in
        \texttt{CoverService::process\_and\_save\_bytes} then worked
        from a lossy source. The new fallback chain
        (\texttt{extraLarge \string-> large \string-> medium
        \string-> small \string-> thumbnail \string-> smallThumbnail})
        picks the best available variant per title, so existing titles
        attach a sharper cover after re-fetch and the rare title that
        only ships \texttt{medium} upward but no \texttt{thumbnail}
        finally attaches a cover at all. Closes the (1) audit piste of
        the 50\,\% prod miss-rate investigation; piste 3 manual upload
        already shipped in v1.7.6 \#335; piste 2 BDGest cover-only
        fetch by ISBN remains open as a separate cycle.
\end{itemize}

\textbf{Admin-visible:}

\begin{itemize}
  \item \textbf{\#334 \string— metadata-chain + provider-health
        timeouts in \texttt{/admin > System}.} Two timeouts that were
        previously hardcoded (per-provider chain timeout) or env-var-only
        (provider-health probe timeout) now appear inside the Metadata
        Providers block of the System tab. Both are bounded
        1..=60 seconds, persisted in the K/V \texttt{settings} table
        (migration \texttt{20260526075659}), and hot-reload through
        \texttt{Arc<RwLock<AppSettings>{}>} so the next chain run /
        next ping round picks up the new value with no restart. The
        legacy environment variables
        \texttt{MYBIBLI\_METADATA\_CHAIN\_PROVIDER\_TIMEOUT\_SECS} and
        \texttt{MYBIBLI\_PROVIDER\_HEALTH\_TIMEOUT\_SECS} are honored
        as one-shot bootstrap via
        \texttt{config::migrate\_legacy\_env\_vars} (mirrors the
        v1.7.1 \#308 log-level pattern). Useful for deployments with
        slow DNS+TLS (rural fiber, VPN, satellite).
\end{itemize}

\textbf{Hardening (no user-visible behaviour change):}

\begin{itemize}
  \item \textbf{\#46 \string— anonymous-session purge survives
        crash-loops.} The daily purge of anonymous-session rows older
        than 7 days previously slept a fixed 24\,h after every boot.
        On a host that crash-looped or rolling-deployed faster than
        24\,h the purge never fired and the \texttt{sessions} table
        grew unbounded. The next-run delay is now computed from a
        persisted \texttt{last\_anonymous\_session\_purge\_at} K/V row
        (migration \texttt{20260526075700}): if 24\,h have already
        elapsed since the previous run, the purge fires immediately
        (catch-up); otherwise it sleeps the remainder. Spec §Task 4.3
        contract preserved for fresh installs.
  \item \textbf{\#28 \string— observability trio.} (a)
        \texttt{LoanService::register\_loan} retry trail now
        distinguishes a non-transient business-rule failure that
        follows a transient retry (e.g.\ concurrent soft-delete
        between attempts that flips the second attempt into a
        BadRequest) — it used to read in the log as
        ``\texttt{warn!("retrying")}'' immediately followed by the
        business-rule error, implying the retry caused it. (b) The
        4 catalog ``fire-and-forget'' \texttt{fetch\_metadata\_chain}
        spawn sites now go through a new
        \texttt{tasks::metadata\_fetch::spawn\_fetch} wrapper that
        captures panics from the inner future and logs them via
        \texttt{tracing::error!} instead of dropping the JoinHandle
        silently. (c) CLAUDE.md single-tenant retry note now explicit
        that \texttt{sqlx::Error::PoolTimedOut} is intentionally
        \emph{not} retried.
  \item \textbf{\#24 \string— \texttt{templates\_audit} hardening
        pass.} Five edge cases closed: (a) \texttt{strip\_html\_comments}
        rewritten as a state machine — an unterminated \texttt{<!--}
        no longer consumes the rest of the source file (previously
        any inline-markup violation past an accidentally-truncated
        comment was invisible to the audit). (b) The same function
        now slices the source \texttt{\&str} instead of casting
        UTF-8 bytes to \texttt{char}, so accented characters in
        failure-report snippets render correctly. (c) New
        \texttt{strip\_svg\_inner\_blocks} masks SVG inner content
        so an inline \texttt{<svg><style>...</style></svg>} can no
        longer false-positive the bare-\texttt{<style>} check. (d)
        The \texttt{<script>} and \texttt{<style>} attribute regex
        now uses a quote-aware grammar
        \texttt{(?:[\textasciicircum>"']|"[\textasciicircum"]*"|'[\textasciicircum']*')*}
        so a literal \texttt{>} inside a quoted attribute value
        cannot terminate the match early. (e) The
        \texttt{src=}/\texttt{type=} script-bypass filter behaviour
        was acknowledged as imprecise but not exploitable; no code
        change needed.
  \item \textbf{\#196 \string— home-search typing-race flake.} The
        long-standing flake at \texttt{tests/e2e/specs/journeys/home-search.spec.ts:224}
        (6 epic retrospectives in a row) finally closed.
        \texttt{simulateTyping} switched from
        \texttt{locator.pressSequentially} to \texttt{keyboard.type}
        after an explicit \texttt{focus()} — the former re-focuses the
        element between key actions and could drop a keystroke under
        default-worker parallelism. The URL-state poll on the test
        was bumped from 2\,s to 5\,s to give the debounced search +
        HTMX swap + \texttt{hx-push-url} chain headroom under load.
\end{itemize}

\section{What's new in 1.7.8}\label{sec:whats-new-1.7.8}

Version 1.7.8 is a hardening patch with one user-visible UX fix and
three code-review-finding hardenings. \emph{No schema migration, no
breaking changes}; routine \texttt{docker compose pull \&\& docker
compose up -d}.

\textbf{User-visible:}

\begin{itemize}
  \item \textbf{\#136 \string— concurrent-delete modals surface
        feedback instead of silently no-op'ing.} Two librarians with
        the same \texttt{/borrower/:id}, \texttt{/contributor/:id},
        or return-loan modal open used to see the loser's Delete
        click vanish silently: the modal handler returned 404, the
        default \texttt{AppError::NotFound} retargeted to
        \texttt{\#feedback-list}, and these pages don't declare that
        slot — HTMX swap silently dropped. The three modal-trigger
        GET handlers now return 200 + inline feedback fragment +
        \texttt{HX-Retarget} to the page's existing slot
        (\texttt{\#borrower-feedback}, \texttt{\#contributor-feedback},
        or the \texttt{?target=} resolution for the return-loan
        flow). The second librarian sees a localized "already
        deleted by another session" toast in all four supported
        locales.
\end{itemize}

\textbf{Hardening (no user-visible behaviour change):}

\begin{itemize}
  \item \textbf{\#39 \string— atomic login session swap.}
        \texttt{services::auth::authenticate\_session} previously
        ran the INSERT new session row and the UPDATE prior
        anonymous session row as two independent queries. A
        partial failure between them left an orphan anonymous
        session row alongside the new authenticated one (both
        carried valid CSRF tokens for the same browser). Both
        queries now run inside a single \texttt{sqlx::Transaction}
        — atomic swap or full rollback.
  \item \textbf{\#47 \string— CSRF test suite coverage gaps.}
        Four high-value Rust integration tests added: HTMX-branch
        language POST, form-field-token logout (the nav-bar shape),
        stale-token replay against a rotated session, and a 403
        body assertion proving the rejection envelope carries the
        FeedbackEntry markup. The remaining 5 E2E items
        (parallel-pollution teardown + selector tightening) are
        deferred to a follow-up E2E pass.
  \item \textbf{\#217 \string— modal Confirm scope tightening.}
        \texttt{modal.js::originatesFromConfirm} used to return
        true for ANY \texttt{<form>} descendant of the open
        dialog. A future modal with a nested form would have
        tagged the nested form's submissions with
        \texttt{X-Modal-Confirm: true} and tripped the
        server-side \texttt{ModalConfirmRetargetGuard}. The
        predicate now matches only the dialog's primary form
        (\texttt{dialog.querySelector("form")}) — the macro's
        Confirm form by construction. An E2E regression-guard
        injects a secondary form into an open dialog and asserts
        the header is NOT sent.
\end{itemize}

\section{What's new in 1.7.7}\label{sec:whats-new-1.7.7}

Version 1.7.7 is a test-rigor patch bundling one user-visible
enhancement and seven code-review-finding clean-ups closing
silent-no-op E2E patterns and locking concurrency / locale-resolution
contracts. \emph{No schema migration, no breaking changes}; routine
\texttt{docker compose pull \&\& docker compose up -d}.

\textbf{User-visible:}

\begin{itemize}
  \item \textbf{\#336 \string— series detail card layout.} The
        \texttt{/series/:id} page used to list each position as a
        compact coloured cell that only carried the volume number.
        Now every filled position renders as a card with the cover
        thumbnail at top (or the media-type icon fallback when no
        cover has been downloaded), a "Vol.\ N" prefix in the
        accent color, the title (truncated), and the primary
        contributor. Missing positions render as dashed-border gap
        cards that still carry the "Vol.\ N" so collectors see at a
        glance which slots are still gaps. The grid is responsive
        (2 columns on mobile scaling to 6 columns on extra-wide
        screens) and preserves the ARIA \texttt{role="grid"} /
        \texttt{role="gridcell"} contract so screen-reader navigation
        is unchanged.
\end{itemize}

\textbf{Test rigor (no user-visible behaviour change):}

\begin{itemize}
  \item \textbf{\#43 \string— DB-snapshot helper on contributor POST
        rejection.} A new \texttt{captureEntityCounts()} Playwright
        helper proves that an anonymous-POST 403/303 rejection left
        the DB untouched (status-code assertion alone could miss a
        future regression where the middleware rejects with the
        right status while the mutation also fires).
  \item \textbf{\#62 \string— real assertions in the admin permanent-
        delete spec.} The previous spec wrapped every assertion in
        \texttt{if (tableExists) \{ \dots \}}, silently no-op'ing
        on a clean CI DB. The rewrite seeds soft-deleted entities
        per-test and exercises the full type-to-confirm friction.
  \item \textbf{\#88, \#89 \string— admin system settings concurrency
        + locale resolution.} Two missing test branches around the
        partitioned optimistic-locking design (different settings
        rows don't collide) and the locale-resolution chain
        (Accept-Language and authenticated user preference both
        win over the global default).
  \item \textbf{\#96 \string— concurrent first-launch race
        (integration test).} Two \texttt{tokio::spawn} Step 1 POSTs
        race the first-admin guard and assert exactly one admin row
        afterwards — the AC13 invariant that was unit-tested by
        shape but never exercised end-to-end.
  \item \textbf{\#119 \string— dashboard indicator E2E seeds its own
        unshelved volume.} The "What needs attention" smoke used to
        early-return on a clean DB. The unshelved indicator is now
        properly seeded via \texttt{scanTitleAndVolume()}; the
        overdue indicator requires backdating a loan timestamp and
        is tracked separately as a follow-up.
  \item \textbf{\#120 \string— single-active-filter contract.} Two
        new render unit tests lock the contract that
        \texttt{?filter=overdue\&q=X\&sort=Y} short-circuits the
        search-fragment branch (the indicator-list owns the
        response; \texttt{\#browse-results} stays in the DOM as the
        HTMX target but carries no search content).
\end{itemize}

\section{What's new in 1.7.6}\label{sec:whats-new-1.7.6}

Version 1.7.6 is a focused patch with two user-visible feature fixes
and three code-review-finding hardening commits. \emph{No schema
migration, no breaking changes}; routine \texttt{docker compose pull
\&\& docker compose up -d}.

\textbf{User-visible:}

\begin{itemize}
  \item \textbf{\#331 \string— media type now editable on
        \texttt{/title/:id}.} A BD or comic catalogued via its ISBN
        used to lock as a "book" forever — the metadata edit form
        displayed \texttt{media\_type} read-only. The edit form now
        renders a dropdown with the six variants (\texttt{book},
        \texttt{bd}, \texttt{cd}, \texttt{dvd}, \texttt{magazine},
        \texttt{report}); changing it triggers an
        \texttt{admin\_audit} entry (\texttt{title\_media\_type\_edit})
        with the before/after pair. Useful for fixing a BD that
        the BnF resolved as a book at scan time, or for any
        misclassification.
  \item \textbf{\#335 \string— manual cover upload.} The
        provider-chain fallback (Open Library Covers API) misses
        most French BDs, mangas, and small-publisher technical books
        because that index is English-publishing-heavy. Click
        \emph{Upload cover} under the cover thumbnail on
        \texttt{/title/:id} (Librarian+) to drop a JPG / PNG / WebP /
        GIF from disk (≤ 10\,MiB). The image goes through the exact
        same decode/resize/JPEG-encode pipeline as provider-downloaded
        covers, so the on-disk artifact stays byte-compatible.
        \texttt{cover\_image\_url} is added to
        \texttt{manually\_edited\_fields}, which makes the bulk-refetch
        admin action \emph{skip} the manually-curated title on
        subsequent runs — your hand-picked cover is preserved.
\end{itemize}

\textbf{Hardening (no user-visible behaviour change):}

\begin{itemize}
  \item \textbf{\#109 \string— recent-additions card test coverage.}
        The home-page render tests previously left
        \texttt{publication\_date} and \texttt{cover\_image\_url}
        as \texttt{None}, never exercising the corresponding
        \texttt{\{\% if let Some(d) = \dots \%\}} branches. A
        wrong format string or stray tag in those branches would
        have slipped past unit tests. A new factory
        \texttt{fake\_search\_result\_full} and a new render test
        close the gap.
  \item \textbf{\#40 \string— CSRF exempt-route normalisation.}
        The exempt-list comparison was a literal string compare;
        \texttt{POST /login/} or \texttt{POST //login} (variants
        Axum's router dispatches identically to \texttt{POST /login})
        would have hit CSRF validation instead of bypassing. New
        \texttt{normalize\_exempt\_path} helper strips trailing
        slashes and collapses repeated slashes — percent-encoded
        forms are intentionally \emph{not} decoded (a
        \texttt{/login\%2F..\%2Fadmin} bypass attempt stays
        rejected).
  \item \textbf{\#36 \string— skip session resolver on static
        assets.} \texttt{session\_resolve\_middleware} previously
        walked the resolver on every request, including
        \texttt{/static/*}, \texttt{/covers/*}, and \texttt{/logo/*}.
        A single home-page load fanned out dozens of asset fetches,
        each triggering a session-row read (and an anonymous-row
        INSERT on a fresh tab). The middleware now short-circuits
        on those prefixes alongside the existing \texttt{/health}
        liveness-probe bypass.
\end{itemize}

\section{What's new in 1.7.5}\label{sec:whats-new-1.7.5}

Version 1.7.5 is a focused patch bundling one user-visible bug fix
and seven code-review-finding hardening commits. \emph{No schema
migration, no breaking changes}; routine \texttt{docker compose
pull \&\& docker compose up -d}.

\textbf{User-visible:}

\begin{itemize}
  \item \textbf{\#325 \string— add / remove contributor silently
        failed on \texttt{/title/:id}.} Clicking
        \emph{+ Add contributor} on a title detail page opened the
        form, accepted name and role, but \emph{Save} produced no
        observable effect. Root cause: the contributor form
        hardcodes \texttt{hx-target="\#feedback-list"}, but the
        slot was only declared on \texttt{/catalog} and
        \texttt{/admin} — not on \texttt{/title/:id}. The same
        defect silently affected the contributor \emph{Remove}
        button. Regression of the v1.7.2 fix that added the title-
        detail flow without porting the slot. Added a
        templates-audit guard that panics if any page references
        \texttt{hx-target="\#feedback-list"} without declaring
        \texttt{id="feedback-list"}.
\end{itemize}

\textbf{Hardening (no user-visible behaviour change):}

\begin{itemize}
  \item \textbf{\#91 \string— form re-render on validation
        error.} Admin → System → Loans/Language saves return 400
        with the form body re-rendered (preserves the typed
        value) + an inline error feedback entry, instead of an
        empty bare 400 that HTMX 2.0 silently dropped.
  \item \textbf{\#42 \string— strict first-child CSRF token.}
        The \texttt{forms\_include\_csrf\_token} audit now
        requires \texttt{\_csrf\_token} as the very first
        \texttt{<input>} of every POST form, not just within the
        first five.
  \item \textbf{\#48 \string— audit Rust HTML literals.} New
        sibling test \texttt{rust\_emitted\_post\_forms\_include\_csrf\_token}
        walks \texttt{src/**/*.rs} and refuses any
        \texttt{<form method="POST">} string literal that does
        not declare its CSRF input nearby. Locks the existing
        \texttt{src/routes/locations.rs:301} site; any new Rust-
        emitted form must also declare its hidden token.
  \item \textbf{\#220 \string— JSON-object HX-Trigger.}
        \texttt{modal\_confirm\_retarget\_guard} now tolerates the
        HTMX JSON-object \texttt{HX-Trigger} shape in addition to
        the comma-separated shape. Defensive: the current emitter
        uses comma-separated.
  \item \textbf{\#31 \string— scanner-guard pipeline respect.}
        The scanner-guard JavaScript that forwards USB-scanner
        bursts to a focused modal text input now honors
        \texttt{maxLength} and skips while an IME composition is
        active. Sub-items 3/4/5 were already closed as N/A to
        the current modal shape.
  \item \textbf{\#90 \string— DRY admin\_system.} The duplicated
        optimistic-lock UPDATE SQL between \texttt{save\_setting}
        and \texttt{run\_provider\_update} is consolidated; the
        latter now delegates and map\_errs the conflict message
        to interpolate the provider label.
  \item \textbf{\#152 \string— anonymous \texttt{/series} test.}
        Adds the missing role-gate test that locks FR95's
        anonymous-readable contract for \texttt{/series}
        (mirrors \texttt{/catalog} and \texttt{/locations}).
\end{itemize}

\section{Rollback}

Rollback is \emph{not} a first-class flow. Schema migrations move
forward only — there is no \texttt{down.sql}. The supported recovery
strategy is:

\begin{enumerate}
  \item Restore the pre-upgrade database backup
        (chapter~\ref{ch:backup}).
  \item Pin the image tag back to the prior version.
  \item Start the stack.
\end{enumerate}

This works because the backup is consistent with the prior schema.
Trying to point an older image at a newly-migrated database will
fail to boot.
